# 외부 접속 허용 (0.0.0.0은 모든 IP 허용)
bind 0.0.0.0

# 레디스 비밀번호 설정 (원하는 비밀번호로 변경하세요)
requirepass mysecretpassword

# 최대 사용 메모리 설정 (예: 512mb)
maxmemory 512mb

# 메모리가 가득 찼을 때 오래된 데이터부터 삭제하는 정책
maxmemory-policy allkeys-lru
